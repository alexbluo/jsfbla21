import { useDispatch, useSelector } from "react-redux";
import FilterBlock from "./FilterBlock";

export default function FilterBlockList() {
  return (
    <div>
      {/* <FilterBlock filter={"placeholder"} /> */}
    </div>
  );
}
